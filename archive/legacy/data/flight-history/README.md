# Disabled flight history

The Flight History route and API were disabled in July 2026. Their source files are
stored as `.txt` so Next.js and TypeScript do not compile them. The associated XLSX
workbooks remain under `data/flown` for reference.
